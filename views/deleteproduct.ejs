<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Delete Product</title>
    <link rel="stylesheet" href="/css/deleteproduct.css">
</head>
<body>

<!-- Navbar -->
<header class="navbar">
    <h1>Cooking Recipes</h1>
    <nav>
        <a href="/">Shop</a>
        <a href="/newproduct">New Product</a>
        <a href="/orders">Orders</a>
    </nav>
</header>

<!-- Main Content -->
<main class="form-container">
    <form action="/deleteproduct" method="POST" class="product-form delete-form">

        <h2>Are you sure you want to delete this product?</h2>

        <div class="product-card">
            <img src="<%= product.image %>" alt="<%= product.name %>">
            <h3><%= product.name %></h3>
            <p class="description"><%= product.description %></p>
            <p class="price"><%= product.price %> EGP</p>
            <input type="hidden" name="_id" value="<%= product._id %>">
        </div>

        <button class="btn delete-btn" type="submit">Delete Product</button>
        <a href="/" class="btn cancel-btn">Cancel</a>
    </form>
</main>

</body>
</html>
